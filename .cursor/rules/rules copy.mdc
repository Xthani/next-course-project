---
alwaysApply: true
---
description:

# Строгая модульная архитектура React-приложения

## 🎯 Цель архитектуры

Данная архитектура предназначена для:
- Построения **масштабируемых**, **поддерживаемых** и **предсказуемых** React-приложений
- Соблюдения **SOLID** и **чёткой модульности**
- Минимизации технического долга при росте проекта
- Разделения обязанностей между слоями приложения
- Единообразного стиля разработки в команде

## 📜 Принципы

1. **Single Responsibility (SRP)** — каждый модуль, компонент или файл отвечает **только за одну задачу**
2. **Open/Closed (OCP)** — функциональность расширяется через новые модули/компоненты, но существующий код не модифицируется без крайней необходимости
3. **Liskov Substitution (LSP)** — заменяемость компонентов без нарушения функционала
4. **Interface Segregation (ISP)** — модуль имеет только необходимые интерфейсы/пропсы, без лишних зависимостей
5. **Dependency Inversion (DIP)** — высокоуровневые модули не зависят от низкоуровневых напрямую; оба зависят от абстракций

## 📂 Структура файлов

```
src/
  app/                     # Глобальные настройки и инициализация приложения
    api/                   # Глобальные API-инстансы и интерсепторы
      axiosInstance.ts
    store/                 # Глобальное состояние (Redux, Zustand, RTK Query)
      store.ts
      authSlice.ts
    providers/             # Глобальные провайдеры (ReduxProvider, ThemeProvider, ErrorBoundary)
      AppProvider.tsx
    layout/                # Глобальные Layout-компоненты
      MainLayout.tsx
      AuthLayout.tsx
    config/                # Глобальные конфиги проекта
      index.ts
    App.tsx                # Корневой роутинг
    index.tsx              # Точка входа

  pages/                    # Страницы (роуты приложения)
    login/
      page.tsx
    admin/
      stocks/
        page.tsx
        page.module.css
        ui/                 # Локальные UI-компоненты страницы
          StocksStats.tsx
          StocksFilters.tsx
        new/page.tsx
        [id]/page.tsx

  features/                 # Отдельные бизнес-фичи
    stocks/
      api/
        stocksApi.ts
        stocks.types.ts
      model/
        useStocks.ts
        useStock.ts
      ui/
        StocksTable.tsx
        StockForm.tsx
        AddStockButton.tsx

  shared/                   # Общие переиспользуемые ресурсы
    ui/                     # Переиспользуемые UI-компоненты
      Button/
        Button.tsx
        Button.module.css
      Table/
        Table.tsx
        Table.module.css
      EntityCount/
        EntityCount.tsx
    hooks/                  # Общие хуки
      useDebounce.ts
      useClickOutside.ts
      useLocalStorage.ts
    utils/                  # Утилиты
      formatDate.ts
      formatPrice.ts
      debounce.ts
    types/                  # Глобальные типы
      index.ts
```

## 🏗 Назначение слоёв

### **`app/`**
- **Единственный источник глобальных зависимостей**
- Глобальные провайдеры, конфиги, инициализация стора, интерсепторы API
- **Запрещено** импортировать из `app` в `shared` или `features`, если это не конфигурация

### **`pages/`**
- **Только страницы** и роутинг
- Должны быть максимально **тонкими**
- Собирают UI из `features` и `shared`
- **Верстка и стили макета страницы** — локально в `page.module.css`
- **Локальные UI-компоненты** (нужные только на этой странице) хранятся в `pages/[route]/ui/`
- **Запрещено**:
  - Писать бизнес-логику прямо в `page.tsx`
  - Обращаться к API напрямую

### **`features/`**
- **Бизнес-функциональность**, объединённая по смыслу
- Каждая фича **полностью автономна**:
  - `api/` — работа с бекендом (fetch, axios, RTK Query)
  - `model/` — локальное состояние, хуки
  - `ui/` — компоненты, связанные только с этой фичей
- **Запрещено**:
  - Использовать код другой фичи напрямую без явного импорта её API/UI
  - Дублировать логику, которая уже есть в `shared`

### **`shared/`**
- Переиспользуемые абстрактные элементы:
  - UI-компоненты (Button, Modal, Table)
  - Хуки (useDebounce, useClickOutside)
  - Утилиты (formatDate)
  - Типы
- **Никогда** не содержит бизнес-логики
- Может использоваться в `features` и `pages`

## 📐 Правила для верстки и стилей

### **Локальные стили страницы**
- Макет страницы (паддинги, гриды, позиционирование) оформляется в `page.module.css`
- Эти стили не должны попадать в глобальные

### **Локальные UI-компоненты страницы**
- Если компонент нужен только для одной страницы → кладём его в `pages/[route]/ui/`
- Если компонент может использоваться на нескольких страницах → кладём в `shared/ui/`
- Если компонент связан с бизнес-логикой конкретной сущности → в `features`

## 📌 Импорты

- `shared` → можно импортировать везде
- `features` → можно импортировать в `pages` и других `features` (с осторожностью)
- `pages` → не импортируются никуда
- `app` → импортируется только на уровне входа и роутинга

## 🚫 Запрещено

- Логика API внутри компонентов UI
- Манипуляции с DOM напрямую, минуя React
- Глобальное состояние без необходимости
- Дублирование кода, который уже есть в `shared`

## 🔧 Дополнительные улучшения

- **Error Boundary** (`app/providers/ErrorBoundary.tsx`) — глобальный отлов ошибок
- **Notifications** (`app/providers/Notifications`) — система уведомлений
- **Config** (`app/config`) — все переменные окружения и флаги фич
- **Permissions** (`shared/lib/permissions.ts`) — централизованная система прав доступа
- **Lazy Loading** (`React.lazy` + `Suspense`) — ленивая загрузка страниц
- **Тестирование** — Jest + React Testing Library, тесты рядом с кодом

## 🔄 Пример потока данных

1. Пользователь открывает `/admin/stocks`
2. `page.tsx` подключает `StocksTable` из `features/stocks/ui`
3. `StocksTable` использует `useStocks()` из `features/stocks/model`, который вызывает `stocksApi` из `features/stocks/api`
4. `stocksApi` использует `axiosInstance` из `app/api` для запросов
5. Ответ рендерится в `shared/ui/Table`

## ✅ Заключение

Эта архитектура:
- **Модульная** — каждая часть изолирована
- **Масштабируемая** — можно добавлять новые фичи без риска поломок
- **Поддерживаемая** — легко тестировать и заменять части системы
- **Строгая** — чёткие правила импорта и ответственности
- **Совместима с SOLID** и принципами чистой архитектуры

Любое отклонение от этих правил должно быть согласовано и задокументировано.
